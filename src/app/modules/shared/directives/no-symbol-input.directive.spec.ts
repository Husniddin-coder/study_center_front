import { NoSymbolInputDirective } from './no-symbol-input.directive';

describe('NoSymbolInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSymbolInputDirective();
    expect(directive).toBeTruthy();
  });
});
